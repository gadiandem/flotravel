import { CustomerBookingInfo } from "../../common/customer-booking-info";
import { GENDER } from "../../enum/gender";

export class HotelCustomerInfo extends CustomerBookingInfo {
    // gender: GENDER;
    // firstName: string;
    // middleName: string;
    // lastName: string;
    // country: string;
    // mobile: string;
    // isNotify: boolean;
    // passport: string;
    roomCode: string;
}