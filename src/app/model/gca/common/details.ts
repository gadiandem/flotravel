import {Contacts} from './contacts';
import {Name} from './name';

export class Details {
  name: Name;
  contacts: Contacts;
  date_of_birth: string;
  passport_no: string;
  comments: string;
  signage: string;
}
