export class Name {
  title: string;
  forename: string;
  surname: string;

  constructor(title: string, forename: string, surname: string) {
    this.title = title;
    this.forename = forename;
    this.surname = surname;
  }
}
